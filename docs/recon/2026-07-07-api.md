# synthwatch-api recon — 2026-07-07

**Scope:** analysis only, docs-only. Branch `analysis/recon-2026-07-07` off `origin/main` @ `bfea595`
(includes #178). No prod DB, no deploy. Six questions; one section each.

**Evidence contract:** every finding cites `file:line` or command output. **OBSERVED** (in source /
command output) is separated from **INFERRED** (reasoning). Load-bearing claims name a falsifier and run
it. ANSWER = ground-truth-or-hypothesis + evidence + caveat.

---

## Q1 — ★S6 Read-surface-as-MCP readiness

### OBSERVED — the read (GET) inventory (44 endpoints)

All GET routes, from `grep -rn 'HttpTrigger(...,"get"...)' Functions/*.cs`:

| Surface | Route | Auth (verb-gate default = **open**) | Pagination / window |
|---|---|---|---|
| Checks | `GET /checks` (list), `/checks/{id}` | anon; `requestHeaders` field session-gated | none (full fleet) |
| Runs | `/checks/{id}/runs` | anon | **keyset cursor** `(started_at DESC,id DESC)` `?cursor&pageSize` (≤200) + `?from&to&outcome` — `ChecksFunctions.cs:356-427` |
| Metrics | `/checks/{id}/metrics` | anon | **offset** `.Skip/.Take` + total — `ChecksFunctions.cs:470-486` |
| Series | `/checks/{id}/availability-series` | anon | `?window=24h\|7d\|30d\|90d` bucketed |
| Incidents | `/incidents`, `/incidents/{id}` | anon | **keyset cursor** `(opened_at DESC,id DESC)` `?from&to&status&checkId&cursor&pageSize` — `IncidentsFunctions.cs:24-76` |
| Reports ×11 | `/reports/{deploys,egress,region-health,slo,mttr,incident-breakdown,trust,trust/{id},availability,performance,narrative}` | anon | `?window=` + `?tag=key:value` (repeatable, AND); windowed aggregates (no cursor) |
| SLA/Status | `/sla`, `/status` | anon | windowed |
| Config reads | `/flows`,`/specs`,`/locations`,`/checks/{id}/locations`,`/tags`(+`/suggested`),`/checks/{id}/tags`,`/routing`,`/channels/{id}/test/status`,`/notifications/health`,`/health` | anon | none |
| Session-gated reads | `/channels`, `/reconcile/plan`, `/reconcile/drift`, `/runs/{id}/trace\|screenshot\|trace-signals`, `/checks/{id}/success-trace` | **editor/admin** via `SessionReadGate` | — |
| Admin reads | `/editors`, `/access-requests` | **admin** (`EditorsFunctions.RequireAdminAsync`) | — |
| Session | `/auth/me` | any valid session | — |

**Pagination discipline (OBSERVED):** the two unbounded-growth tables use **keyset cursors** (runs `ChecksFunctions.cs:357`; incidents `IncidentsFunctions.cs:25`) — MCP-safe (stable, no OFFSET drift). One outlier: `/checks/{id}/metrics` uses **offset** paging (`ChecksFunctions.cs:481`). Reports are window-bounded aggregates.

### OBSERVED — read auth model

- **Verb-gate default:** `AuthGate.Decide` returns `Allow` for every non-mutating verb — `AuthGate.cs:79` *"reads are always open (read-only default)"*. So GETs are open unless a handler self-guards.
- **Sensitive reads self-guard** via `SessionReadGate.RequireSessionAsync` (`SessionReadGate.cs:24-32`): resolves the caller with `IAuthPrincipal.FromBearerAsync(req.Headers.Authorization, ct)` → **401** if no valid session, **403** if live role isn't editor/admin (`CanWrite` floor).
- **The token is an opaque BEARER token**, not a cookie: `FromBearerAsync` → `AuthTokens.BearerFrom(authorizationHeader)` → `sessions` lookup by token hash → `{email, live role}` (`AuthPrincipalService.cs:34-48`). Role is re-resolved live on every request.

### INFERRED — MCP composability

Because auth is **`Authorization: Bearer <opaque-token>` with server-side session lookup and no cookie/CSRF/Origin dependency**, a headless MCP caller composes cleanly: mint a session token (OTP flow, or the admin-session recipe in `CLAUDE.md`) and forward it as a bearer header. The read surface is **two-tier**: the bulk (aggregates, status, metrics) is anon — wrappable with no token at all; the sensitive tier (channel creds, reconcile SQL, forensic artifacts) needs an editor/admin bearer. An MCP token model maps 1:1 onto the existing bearer/role floor — no cookie-session fight.

**Falsifier (run):** *"a read requires a browser cookie / CSRF token, so a headless bearer can't authenticate."* → `grep -rniE "Cookie|CSRF|SameSite|XSRF|antiforgery" Infrastructure/*.cs Functions/*.cs` → **no matches** (only bearer). Claim survives: reads are bearer-or-anon, headless-friendly. The one live check to fully confirm (not run here — no prod): `curl -H "Authorization: Bearer <token>" /api/channels` → 200; anon → 401 (the recipe already documented in `docs/auth-gates.md:52-58`).

### VERDICT — Q1

**The read surface is complete + stable enough to wrap as MCP tools for most queries WITHOUT new endpoints — with two gaps, one of them load-bearing.**

Already answerable via existing endpoints:
- **"p95 for check X over 7d"** → `GET /checks/{id}/metrics` (carries p50/p95) or `GET /reports/performance?window=7d` (per-check p95, `groupBy`). ✅ the *check-X / 7d* part.
- Availability/uptime %, SLA headline, MTTR, incident breakdown, trust/regression scorecard, deploy list, region freshness, per-run history — all direct GETs.

Would need a NEW endpoint (or client-side MCP orchestration):
- **"…in env Y"** — ❌ **no environment dimension exists anywhere** (see Q2 falsifier). No GET accepts `?env`; no `environment` column on checks/runs. This is not wrappable — it needs the schema/dimension work in Q2 first.
- **"which monitors regressed near the last deploy"** — ⚠️ *composable but not single-shot*: `GET /reports/deploys` gives deploy times and `GET /incidents/{id}` carries `nearbyDeploys` (#157), and per-check regression is in `/reports/trust`, but **no single endpoint returns "monitors whose p95/status regressed within N min of the last deploy."** An MCP tool can orchestrate it from 2-3 calls; a first-class endpoint would be a convenience, not a prerequisite.

**Bottom line:** wrap-ready for the fleet/check/window/incident/report queries today (bearer or anon). The **"env Y" axis is the one true blocker** and is a data-model gap (Q2), not an endpoint-shape gap.

## Q2 — ★S1 SLO/report environment-partitionability

### OBSERVED — the SLO aggregation grouping keys

`GET /reports/slo` (`ReportsFunctions.cs:184-198`):
```sql
FROM checks c
CROSS JOIN LATERAL slo_status(c.id, {from}, {to}) s      -- per-check budget/burn, window args
CROSS JOIN LATERAL slo_burn_status(c.id) b               -- per-check page-state (fixed 1h/6h/30m)
WHERE c.slo_target IS NOT NULL
  AND (cardinality({tags}) = 0 OR c.id IN (... check_tags AND-match ...))
ORDER BY c.name
```
- **The only grouping key is `check_id`** (one row per SLO-having check). `slo_status(c.id, from, to)` and `slo_burn_status(c.id)` are runner-owned DB functions keyed **solely on `check_id` + window** — no env argument at any layer.
- The fleet rollup is `SloReportProjection.Build(rows)` over **every** returned row (all `slo_target IS NOT NULL` checks).
- The **only partition dimension that exists is `tag`** (`?tag=key:value`, repeatable), and it is **include-only** (`c.id IN (checks having ALL selected tags)`) — there is no exclude/NOT-tag path.
- Same shape in `/reports/mttr` (`ReportsFunctions.cs:238-248`), `/reports/incident-breakdown`, `/reports/trust`: `FROM checks/incidents … JOIN checks … [optional AND-tag include]`, grouped by `check_id`. No env dimension.

### OBSERVED — the environment falsifier

**Falsifier (run):** *"there is an `environment` dimension the aggregation could exclude on."*
`grep -rniE "environment|\benv\b" Data/Entities/*.cs Data/SynthWatchDbContext.cs` → the **only** hit is `EnvironmentRegional` (`ReportingRows.cs:97`, `SynthWatchDbContext.cs:468`), which is an **RCA classification bucket**, not a deploy env: `count(*) FILTER (WHERE i.rca->>'classification' = 'environment-regional')` (`ReportsFunctions.cs:407`) — one of {real-outage, flaky-transient, selector-drift, environment-regional, perf-regression, unclassified}. Fixture schema has **no** `environment` column on `checks`/`runs`. → Falsifier fails to find an env dimension: **none exists.**

### VERDICT — Q2

**Env-partition is NOT a pure filter addition, but also NOT a full rearchitecture — it sits in between, and the cleanest form is a small schema add + a default-exclude, because the aggregation is keyed on `check_id` only with `tag` as the sole (include-only) partition axis.**

Three honest options, cheapest→soundest:
1. **Tag-as-env (no schema change), but blocked by include-only semantics.** Tags already partition (`env:preprod`). But today's filter can only *include* checks with a tag — to keep pre-prod OUT of the **default (no-tag) prod fleet SLO**, you'd need a **NOT-tag / default-exclude** clause. That's a **query/filter addition** (add a negative-tag predicate + make prod aggregations default to `env != preprod`), a few lines per report query — no schema, no runner change. Weakness: env-as-tag is a soft convention (a mistagged/untagged pre-prod check silently pollutes prod).
2. **First-class `checks.environment` column (runner-owned schema).** A real dimension the default prod aggregation excludes on (`WHERE coalesce(environment,'prod')='prod'`). This is a **runner migration** (the runner owns the schema — see `CLAUDE.md`) + read-side filter here. Soundest; not a rearchitecture (the aggregation already groups per-check — you're adding a WHERE, not reshaping the rollup).
3. Neither `slo_status`/`slo_burn_status` needs restructuring — both already take `check_id`; env-exclusion happens in the *outer* `FROM checks` predicate, before the LATERAL. So the burn/page path the runner shares is untouched.

**Load-bearing caveat:** because the fleet rollup today includes **every** `slo_target IS NOT NULL` check with no env filter, **a pre-prod check with an SLO target pollutes the prod budget the moment it exists** — the partition must be *default-exclude*, not opt-in, to actually protect prod. Option 1's include-only tag filter does **not** achieve that without the added negative predicate.

## Q3 — Per-location GRID phantom bug (plan T6.8)

### OBSERVED — `CheckSummaryDto` per-location derivation (origin/main @ bfea595)

`ListChecks` (`GET /checks`), `ChecksFunctions.cs:85-99`:
```csharp
var latestPerLocation = await _db.Runs.AsNoTracking()          // :85  ← RUNS, not check_locations
    .Where(r => ids.Contains(r.CheckId))
    .GroupBy(r => new { r.CheckId, r.Location })                // :88  group over runs history
    .Select(g => g.OrderByDescending(r => r.StartedAt).First())
    .ToListAsync(ct);
var byCheck = latestPerLocation.GroupBy(r => r.CheckId)...;     // :90
var rollupByCheck = byCheck.ToDictionary(kv => kv.Key,          // :95
    kv => kv.Value.Select(r => new LocationStatusDto(           // :98  one {location,status} PER RUN-LOCATION
        string.IsNullOrEmpty(r.Location) ? "default" : r.Location, r.Status))...);
```
Assigned into the DTO at `ChecksFunctions.cs:127` (`rollupByCheck.GetValueOrDefault(c.Id, emptyRollup)`).

**Falsifier (run):** *"the summary path keys on `check_locations` (already fixed)."* → `grep -n "check_locations\|CheckLocations" Functions/ChecksFunctions.cs` in the ListChecks path → the only `check_locations` references are the **create-path INSERT** (`:231,:251`) and the **detail** helper `CheckLocationsRollupAsync` from #178 (`:305`). The list/summary rollup (`:85-99`) **never** touches `check_locations`. → Falsifier fails: the grid is runs-keyed.

### VERDICT — Q3

**Real and still OPEN.** #178 fixed only the check-**detail** DTO (`CheckLocationsRollupAsync`, keyed on `check_locations`); the **grid** (`CheckSummaryDto.Locations`) still derives per-location status from **runs history** (`GroupBy(CheckId, Location)` over `runs`), so a location the check was dropped from — whose stale runs remain — still appears in the grid rollup with its last (possibly `fail`) status, exactly the class #178 closed for detail. The fix mirrors #178 but batched for the list endpoint: drive the rollup from `check_locations` for all listed checks (LEFT JOIN latest run), instead of grouping `runs`. Scope: one endpoint, one query rewrite + the existing `LocationStatusDto`/`Pending` semantics already in the tree.

## Q4 — Program.cs enforcement comment (plan T6.12, ?dup of #175)

### OBSERVED — origin/main @ bfea595

`Program.cs:67-70`:
```csharp
// exception shielding, then the authorization gate — INSIDE shielding so a session-lookup error becomes a
// shielded 500 = DENIED (fail-closed). The gate ENFORCES by default — inert only when
// AUTH_ENFORCEMENT_ENABLED is EXPLICITLY "false"/"0" (fail-closed: #161 runtime default ON-when-unset, #173 bicep default true).
```
The comment already says **"ENFORCES by default"**, not "default OFF". `git log -- Program.cs` shows **#175** (`1b106e7`, *"fix stale 'default OFF' enforcement comment in Program.cs (post-#161/#173)"*) is merged. **Falsifier (run):** `grep -niE "default off|default false" Program.cs` → **NONE**.

### VERDICT — Q4

**CLOSED — this is a duplicate of #175, which already landed on main.** No comment PR remains. T6.12 can be marked done.
